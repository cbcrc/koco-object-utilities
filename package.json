{
  "name": "koco-object-utilities",
  "version": "1.2.1",
  "homepage": "https://github.com/cbcrc/koco-object-utilities",
  "authors": [
    "fcharest <frederik.charest@radio-canada.ca>"
  ],
  "description": "Object utilities to be used in a koco project.",
  "keywords": [
    "object",
    "utilities",
    "koco"
  ],
  "scripts": {
    "build": "rm -rf dist && mkdir dist && cp -r src/* dist && rm -rf dist/*.js && babel src --out-dir dist --modules umd --presets es2015,stage-0"
  },
  "main": "src/object-utilities.js",
  "license": "MIT",
  "dependencies": {
    "koco": "^1.0.0",
    "lodash": "^4.13.1"
  },
  "devDependencies": {
    "babel-cli": "^6.9.0",
    "babel-preset-es2015": "^6.9.0",
    "babel-preset-stage-0": "^6.5.0"
  }
}
